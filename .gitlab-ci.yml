pages:
  stage: deploy
  script:
    - mkdir -p public
    - cp index.html gitlab-pat-setup.html wsl-setup.html public/
  artifacts:
    paths:
      - public
  rules:
    - if: $CI_COMMIT_BRANCH == "main"
