# v0.4.0 — PreCompact hook 추가 및 CLAUDE.md iShare 배포 트리거

**날짜**: 2026-05-19
**버전 유형**: MINOR

---

## 변경 내용

### 신규
- `.claude/hooks/pre_compact_review.py` — PreCompact hook 신규 생성. `/compact` 실행 직전 실질적 파일 편집이 있으면 project-memory 회고 리마인더 주입. `trigger == "auto"` 시 조용히 통과, `manual` 시에만 발동. 새 편집마다 재발동(Stop hook과 달리 세션당 1회 제한 없음).
- `RELEASE_MANAGEMENT_GUIDE.md` — `### PreCompact hook (압축 전 리마인더)` 섹션 추가 (7단계 내)
- `CLAUDE.md` — `## iShare 배포` 트리거 섹션 추가 (update subcommand + ISHARE_TOKEN 주의사항)

### 변경
- `.claude/settings.json` — `PreCompact` 이벤트 등록
- `.claude/hooks/session_start_brief.py` — `_cleanup_stale_stop_markers()` → `_cleanup_stale_markers()`로 rename, `.session-compact-reviewed-*` 마커 정리 패턴 추가
- `RELEASE_MANAGEMENT_GUIDE.md` — SessionStart hook 코드 내 cleanup 함수 갱신, 파일 트리에 `pre_compact_review.py` 추가, 표·체크리스트 문구 갱신
- `index.html` — `/sync-index` 동기화
- `README.md` — 7단계 설명 갱신
- `project-memory/components.md` — 파일 트리 + 의존 관계 갱신

---

## Semver 해석 (이 저장소 기준)

| 세그먼트 | 의미 |
|----------|------|
| MAJOR | 가이드 방향 전환 (체계 전면 개편) |
| MINOR | 섹션 추가 또는 주요 개정 |
| PATCH | 오탈자, 링크, 표현 수정 |
